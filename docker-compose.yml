services:
  ns3-seguranca-web:
    build: .
    container_name: ns3-seguranca-dashboard
    ports:
      - "5000:5000"
    volumes:
      - ./scratch:/workspace/scratch
      - ./scripts:/workspace/scripts
      - ./webapp:/workspace/webapp